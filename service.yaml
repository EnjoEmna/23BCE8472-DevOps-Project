apiVersion: v1
kind: Service
metadata:
  name: corporate-website-service
spec:
  selector:
    app: corporate-website
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort